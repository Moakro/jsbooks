---
import Base from "../../../layouts/Base.astro";
import { getCollection } from "astro:content";

const entries = (await getCollection("people")).sort((a, b) =>
  a.id.localeCompare(b.id, "ko"),
);
---

<Base title="인물 — jsbooks">
  <h1>인물</h1>
  <p class="meta">{entries.length}명</p>
  <ul class="index-list">
    {entries.map((e) => (
      <li>
        <a href={`/archive/people/${encodeURIComponent(e.id)}/`}>
          {e.data.name}
          {e.data.name_hanja && (
            <span class="meta"> ({e.data.name_hanja})</span>
          )}
        </a>
      </li>
    ))}
  </ul>
</Base>
