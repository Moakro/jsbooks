---
import Base from "../../../layouts/Base.astro";
import { getCollection } from "astro:content";

const entries = (await getCollection("terms")).sort((a, b) =>
  a.id.localeCompare(b.id, "ko"),
);
---

<Base title="사상 용어 — jsbooks">
  <h1>사상 용어</h1>
  <p class="meta">{entries.length}건</p>
  <ul class="index-list">
    {entries.map((e) => (
      <li>
        <a href={`/library/terms/${encodeURIComponent(e.id)}/`}>
          {e.data.name}
          {e.data.name_hanja && (
            <span class="meta"> ({e.data.name_hanja})</span>
          )}
        </a>
      </li>
    ))}
  </ul>
</Base>
